//TODO Add other ports to tor config file on docker
//   const ports = ["9050", "9052", "9053", "9054"];
//   const randomPortIndex = Math.round(ports.length-1 * Math.random());
//   const arg = `"--proxy-server=socks5://127.0.0.1:${ports[randomPortIndex]},`;
//   console.log(arg);
